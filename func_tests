import numpy as np
import csv
import tkinter as tk
from tkinter import ttk
from tkinter import filedialog as fd
from linkedlist import Node, LinkedList


def main():
        links = LinkedList()
        links.insertTop("this")
        links.insertEnd("list")
        links.insertTop("is")
        links.insertEnd("cool?")
        links.insertIndex("very", 3)
        links.printList()
if __name__ == "__main__":
    main()